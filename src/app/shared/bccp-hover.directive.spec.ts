import { BccpHoverDirective } from './bccp-hover.directive';

describe('BccpHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BccpHoverDirective();
    expect(directive).toBeTruthy();
  });
});
